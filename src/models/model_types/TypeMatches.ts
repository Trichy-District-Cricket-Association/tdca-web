export type TypeMatches = {
    leagueMatch: number;

    institutionMatch: number;

    schoolMatch: number;

    knockoutMatch: number;

    tncaMatch: number;

    combinedDistrictMatch: number;

    interDistrictMatch: number;

    privateMatch: number;
};
